#include "pch.h"

TESTFUNC(stats)
{
	printf( "sizeof(nuStyleAttrib) = %d\n", sizeof(nuStyleAttrib) );
	printf( "sizeof(nuStyle) = %d\n", sizeof(nuStyle) );
	printf( "sizeof(nuDomEl) = %d\n", sizeof(nuDomEl) );
}
