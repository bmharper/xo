#pragma once

#include "nuDefs.h"

/*
// Job for nuQueue
class NUAPI nuJob
{
public:
	virtual ~nuJob();
	virtual void Run() = 0;
};

// Job queue
class NUAPI nuJobQueue
{
public:
			nuJobQueue();
			~nuJobQueue();
	void	Add( nuJob* job );
				
protected:
	TAbcQueue<nuJob*> Queue;
};
*/